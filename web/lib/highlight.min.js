/*! highlight.js v11.9.0 - stub for offline development.
    Replace this file with the real highlight.min.js from:
    https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.9.0/highlight.min.js
*/
var hljs = (function() {
  "use strict";
  var _registered = {};
  function highlight(code, options) {
    return { value: _escHtml(code), language: options?.language || "" };
  }
  function highlightElement(el) {
    if (el) el.classList.add("hljs");
  }
  function highlightAll() {
    document.querySelectorAll("pre code").forEach(highlightElement);
  }
  function registerLanguage(name, fn) {
    _registered[name] = fn;
  }
  function getLanguage(name) {
    return _registered[name] || null;
  }
  function _escHtml(s) {
    return String(s).replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;");
  }
  return {
    highlight: highlight,
    highlightElement: highlightElement,
    highlightAll: highlightAll,
    registerLanguage: registerLanguage,
    getLanguage: getLanguage,
  };
})();
