Long Shots
- Implement my own custom voice
- Active avatar person/face
- Add vision so I can copy/paste images
- Add Text to Image creation
- Text to audio/music?
- Voice to Voice?  Not sure what it's called
- Add STT/Mic button
- Mobile / safari friendly
- Maybe make sure the LLM always knows the date and time?
- Crazy themes

Quick Wins
- Themes: Forest and Rose need to look better. Need an additional light theme that is slightly darker than the 'light' theme.  
- Settings > Developer: Needs to show a loading indicator while the containers/APIs are loading (they don't come up immediately). There should also be a refresh button in the panel since the numbers don't update in realtime.  
- Main chat screen: There's no loading indicator while the LLM is thinking.  We absolutely need to implement something fresh and modern here.
- Main chat screen: LLM responses don't flow in (word for word), it waits until the entire thing is done and then spits it out.  
- File vault panel: Need to implement better indexing status.  Right now it just shows 'indexing...' a static text. It would be great if it showed the progress, if possible.  If too complex to implement, then at least some sort of active indicator.
- Main chat screen: when web search is enabled, there should be some sort of static alert message (perhaps right above the globe) stating "CAUTION: Internet will be used for this query" (or something similar).  
- File vault panel: The "drop files here or browse" should be enlarged so that you can drop files in most areas of the panel. 