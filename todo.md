
- The logo isn't persistent

- Main Screen: When clicking a reference file in a chat, it doesn't display pretty like when its clicked on in the file vault... 

- Main screen: Don't need a massive line for context used.  Perhaps we use a small pie like Claude uses instead. 

- Settings > Users: 
  - should have a "Name" field as well. It should auto populate the users' profile name and also update if the user updates it on their end. 
- Users should have the user's Avatar

- Settings > Interface:
  - The logo isn't persistent
  - Favicon upload needs to be an option

- When updating the Interface configuration (name & logo) The login screen needs to reflect this as well. 

- Logout confirmation button doesn't match the standard
