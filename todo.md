# To do now

CHAT:
- TTS - still doesn't always read through an entire prompt response.  I think some voices get caught up on emotions.  
- Doesn't unload orpheus if using a larger LLM that doesn't fit both the LLM and orpheus.  It needs to do thi and provide a notification to the user if auto TTS is on or if the play button is pressed.  

IMAGE:

MUSIC:

VIDEO:

NOTE TAKER:

CODE STUDIO:

GENERAL:

GPU Manager

# Deferred until user requests

- 3D modeling module?

  
  