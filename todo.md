Settings > Appearance:
  - Need additional text size options...maybe a slider.
  - Logo subtext needs to call out what the optimum size is, in PX
  - 5 preset themes should be an option, then each color can be customized if the user chooses. This should be user/profile relevant and persist.  
  - Administrator should be able to set defaults for all new accounts
Settings > Voice: Voice Preview option should be available.  Description of each voice as well.  
Settings > Developer (should be admin only menu): Needs to be more compact, there is too much vertical space between sections and not consistent with other menus
Settings > Administration: 
 - No need for change my password since it can be done under profile
 - This is where the default settings for all profiles can be configured
 - Buttons need to match buttons under the profile page
Main Screen: 
 - Settings can just be a gear (make it larger) and logout should just be an SVG.  no text, but it should confirm. 
Prompt response: Get rid of the follow-up questions.  
- The default LLM model st